# =============================================================================
# Automedon Portfolio - Environment Configuration Example
# =============================================================================
# Copy this file to .env and customize the values for your deployment.
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your settings
#   docker compose up -d
# =============================================================================

# -----------------------------------------------------------------------------
# Docker Configuration
# -----------------------------------------------------------------------------

# Docker image to use
# Default: makros24/automedon:latest
DOCKER_IMAGE=makros24/automedon:latest

# Container name
CONTAINER_NAME=automedon-portfolio

# Restart policy (no, always, on-failure, unless-stopped)
RESTART_POLICY=unless-stopped

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------

# Application environment (development, production)
NODE_ENV=production

# Server hostname (0.0.0.0 to accept all connections)
HOSTNAME=0.0.0.0

# External port (host machine port)
PORT=3000

# Internal port (container port, usually keep as 3000)
PORT_INTERNAL=3000

# Application URL (used for generating links)
APP_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Portfolio Data Configuration
# -----------------------------------------------------------------------------

# Path to portfolio data directory (relative to docker-compose.yml)
# This directory should contain locale-specific subdirectories (en/, fr/, de/, ar/)
PORTFOLIO_DATA_PATH=./portfolio-data

# Optional: Path for logs directory
# LOGS_PATH=./logs

# -----------------------------------------------------------------------------
# Language Settings
# -----------------------------------------------------------------------------

# Default language code (en, fr, de, ar, or other supported locales)
DEFAULT_LANGUAGE=en

# Enable automatic language detection based on browser settings
ENABLE_LANG_DETECTION=true

# -----------------------------------------------------------------------------
# Feature Flags
# -----------------------------------------------------------------------------

# Enable debug mode (shows additional logging)
DEBUG_MODE=false

# Show grid overlay (for layout debugging)
SHOW_GRID=false

# -----------------------------------------------------------------------------
# Resource Limits
# -----------------------------------------------------------------------------

# CPU limits (e.g., 0.5, 1.0, 2.0)
CPU_LIMIT=1.0
CPU_RESERVATION=0.5

# Memory limits (e.g., 256M, 512M, 1G)
MEMORY_LIMIT=512M
MEMORY_RESERVATION=256M

# -----------------------------------------------------------------------------
# Health Check Configuration
# -----------------------------------------------------------------------------

# Health check interval
HEALTHCHECK_INTERVAL=30s

# Health check timeout
HEALTHCHECK_TIMEOUT=10s

# Number of retries before marking as unhealthy
HEALTHCHECK_RETRIES=3

# Grace period before starting health checks
HEALTHCHECK_START_PERIOD=40s

# -----------------------------------------------------------------------------
# Logging Configuration
# -----------------------------------------------------------------------------

# Log driver (json-file, syslog, journald, gelf, etc.)
LOG_DRIVER=json-file

# Maximum log file size
LOG_MAX_SIZE=10m

# Maximum number of log files to keep
LOG_MAX_FILE=3

# -----------------------------------------------------------------------------
# Network Configuration (Optional)
# -----------------------------------------------------------------------------

# Custom network name (uncomment if using custom networks in docker-compose.yml)
# NETWORK_NAME=automedon-network

# =============================================================================
# Examples for Different Environments
# =============================================================================
#
# Development:
#   NODE_ENV=development
#   DEBUG_MODE=true
#   PORT=3001
#
# Production (Standard):
#   NODE_ENV=production
#   DEBUG_MODE=false
#   APP_URL=https://your-domain.com
#
# Production (High Traffic):
#   CPU_LIMIT=2.0
#   MEMORY_LIMIT=1G
#   LOG_MAX_SIZE=50m
#
# Custom Portfolio Data:
#   PORTFOLIO_DATA_PATH=/var/www/portfolio-data
#
# =============================================================================
